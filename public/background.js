// // 监听消息
// chrome.runtime.onMessage.addListener(function (request, sender, sendResponse) {
//   if (request.type === "cacheIcon") {
//     // 获取站点URL
//     var url = request.url;
//     // 缓存站点图标
//     cacheSiteIcon(url);
//   }
// });
